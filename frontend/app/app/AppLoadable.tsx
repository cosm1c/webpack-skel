import * as React from 'react';
import {AppMainConnected} from './AppMainConnected';

export default class AppLoadable extends React.Component {
  render() {
    return (
      <AppMainConnected/>
    );
  }
}
